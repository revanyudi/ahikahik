# KaguraMD

FOR INSTALL TERMUX
```$ pkg update && pkg upgrade
$ pkg install bash
$ pkg install nodejs
$ pkg install libwebp
$ pkg install ffmpeg
$ termux-setup-storage
$ cd /sdcard/namefile
$ npm start```
